# desktolls 使用说明

## 程序位置

- 主程序：`D:\DESK\desktolls.exe`
- 当前状态：已在系统托盘后台运行
- 设置文件：`%LocalAppData%\desktolls\settings.json`
- 错误日志：`%LocalAppData%\desktolls\desktolls-error.log`（仅发生错误时生成）

主程序是 Windows x64 自包含单文件，不依赖 `D:\DESK\.tools` 中的构建工具即可运行。

底部“退出时恢复设置”默认勾选。通过“退出”按钮或托盘菜单结束程序时，desktolls 会显示桌面图标和任务栏、恢复 Windows 11 默认右键菜单，并恢复它自己管理的 Windows 更新策略。若更新策略恢复所需的 UAC 被取消或状态校验失败，程序会拒绝退出并显示原因；“隐藏到托盘”和关闭窗口不会触发恢复。

## 功能

窗口顶部提供“系统”“输入与性能”“下载”三个页签。系统与桌面相关开关位于“系统”，连点、提示音和内存优化位于“输入与性能”，自定义下载器位于“下载”；切换页签会自动回到该页顶部。

### 桌面图标中键切换

开关打开时，将鼠标移到桌面空白处或桌面图标区域，按一下鼠标中键即可隐藏或显示桌面图标。鼠标位于普通窗口、任务栏或菜单上时不会触发。

“随 Windows 启动”默认打开。系统登录后 desktolls 自动进入托盘，并默认隐藏桌面图标。

### Win10 经典右键菜单

开关打开时，Windows 11 使用 Win10 样式的完整右键菜单；关闭后恢复 Windows 11 默认菜单。切换时 Explorer 会自动重启，因此已经打开的文件资源管理器窗口会关闭，桌面和任务栏会短暂刷新。

### 自动隐藏任务栏

打开开关后，Windows 任务栏会自动收起；把鼠标移动到任务栏所在的屏幕边缘即可临时显示。关闭开关后任务栏恢复常驻显示。该功能调用 Windows Shell 的任务栏接口，不修改注册表、不重启 Explorer，也不需要管理员权限；界面会以 Windows 当前真实状态为准。

### 禁止 Windows 自动更新

此开关使用微软正式的计算机策略，禁止 Windows 自动下载和安装更新，没有“只能暂停几周”的期限。打开后仍可在 Windows 设置中手动检查更新。

- 第一次打开时会出现 Windows UAC，请确认发布路径为 `D:\DESK\desktolls.exe` 后点击“是”。
- 主程序不会以管理员权限常驻；只有一次性策略子进程会提权。
- 关闭开关时会再次请求 UAC，并恢复启用前备份的系统策略值。
- 已经下载或正在安装的更新不一定会被取消。
- 禁止自动更新会降低系统及时获得安全补丁的能力，建议定期手动检查更新。

该功能不停止 `BITS`、`wuauserv` 或 `UsoSvc`，不禁用计划任务，不修改 hosts、服务权限或系统 DLL，因此不会连带破坏 Microsoft Store 和系统修复组件。

### 自动内存优化

- 默认状态：开启
- 默认间隔：`10 秒`
- 可选间隔：`10 秒`、`30 秒`、`1 分钟`、`5 分钟`、`10 分钟`
- 点击“立即优化”可单独执行一次，不受自动开关影响。

该功能只裁剪 `desktolls.exe` 自身的 Windows 工作集，不枚举或操作其他进程，不清理系统待机内存，不申请管理员权限。隐藏到托盘时也会立即执行一次。

任务管理器中的物理内存占用会明显下降，但“私有工作集/提交大小”不一定同步下降；后续功能使用到已裁剪页面时，Windows 会按需重新载入。若频繁使用设置窗口时感觉页面重新载入，可把间隔调大。

### 鼠标左键连点

- 默认触发键：`F8`
- 默认频率：`10 次/秒`
- 按一次触发键开始连点，再按一次停止。
- “触发按键”下拉框可修改全局热键。
- 滑块或两侧的 `−` / `+` 可把频率调整为 `1–100 次/秒`。

若选择的按键已被其他软件占用，desktolls 会保留原热键并提示冲突。

### 复制/粘贴提示音

- “复制提示音”和“粘贴提示音”默认分别开启，可独立关闭。
- 按下标准物理键盘组合键 `Ctrl+C` 时播放轻微上扬音，按下 `Ctrl+V` 时播放较低的落定音。
- 每次物理按下一次只播放一次；长按 C 或 V 不会随着键盘自动重复连续播放。
- 设置中的扬声器按钮可试听对应音效，不受开关状态影响。

该功能只判断按键组合，不拦截按键，也不读取、记录或上传剪贴板内容。声音表示组合键已按下，不代表目标软件一定成功完成复制或粘贴。带有 `Shift`、`Alt` 或 Windows 键的组合不会触发，软件模拟的按键也不会触发。

### 自定义文件下载

1. 粘贴完整的 `HTTP` 或 `HTTPS` 下载地址。程序会自动读取服务器文件名、重定向地址、文件类型和文件头，并填写正确名称与后缀。
2. “自动识别文件名”默认选中。需要自定义名称时取消选中，即可手动编辑文件名。
3. 选择 `单线程`、`2 线程`、`4 线程` 或 `8 线程`，默认 `4 线程`。
4. 点击“开始下载”。下载过程中可查看百分比、文件大小、速度和分段数，也可随时取消。
5. 完成后点击“打开文件夹”查看文件。

服务器支持 HTTP Range 时，desktolls 会并行下载多个分段；不支持时会自动改用单线程。每个分段最多重试 3 次。下载完成前只写入 `.desktolls.part` 临时文件，取消或失败会自动清理。

自动识别优先使用服务器返回的 `Content-Disposition` 文件名，其次使用重定向后的真实网址，再结合 `Content-Type` 和文件头判断扩展名。常见的 EXE、MSI、ZIP、7Z、RAR、PDF、Office 文档、图片、音频和视频格式均可识别。服务器没有提供足够信息时不会强行猜测不可靠的后缀。

需要登录、浏览器 Cookie、验证码或网盘客户端的地址可能返回 `403` 等错误，这类地址不能作为普通直链下载。详细研究与实现边界见 `D:\DESK\desktolls-自定义下载说明.md`。

## 窗口和托盘

- 点击窗口右上角关闭按钮或“隐藏到托盘”，程序继续后台运行。
- 双击 `D:\DESK\desktolls.exe`，或双击托盘图标，可重新打开设置窗口。
- 点击“退出”或托盘菜单中的“退出”，才会停止全部后台功能。

## 恢复和移除

1. 保持底部“退出时恢复设置”勾选，然后点击“退出”并按需确认 UAC。
2. 等待程序显示桌面图标和任务栏、恢复 Windows 11 右键菜单及 desktolls 管理的更新策略后自动退出。
3. 若不希望下次登录自动启动，再重新打开程序并关闭“随 Windows 启动”。
5. 删除 `D:\DESK\desktolls.exe`；需要同时清除设置时，再删除 `%LocalAppData%\desktolls`。
